# Unified Finance Integration API — AI Agent Instructions

1. Follow all parent instructions (`../GEMINI.md`, `~/.gemini/GEMINI.md`).
2. Follow this repository's `AGENTS.md`.
3. Preserve strict P0/P1 delivery boundaries (no P1 features in P0 scopes).
4. No secrets or real credentials in code or `.env`.
5. **REQUIRE EXPLICIT USER APPROVAL** before any:
   - commit
   - push
   - Pull Request (create/merge)
   - branch deletion
   - or any remote action.
